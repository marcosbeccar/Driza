specVersion: "v1"
serviceId: "driza"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "driza-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
