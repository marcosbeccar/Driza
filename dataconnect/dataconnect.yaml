specVersion: "v1"
serviceId: "remart"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "remart-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
